# Local backups (originals before the menu was removed) — kept on disk, not published
*.bak

# Local helper / server logs
server.log

# Distribution zip (uploaded to GitHub via the web, not tracked in git)
tallykhata-home.zip

# Local page generator (not published)
_build/
